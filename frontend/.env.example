# API base URL for production builds. Leave empty when the API is on the same
# Vercel deployment (default vercel.json sets VITE_API_BASE="").
# Set to your backend URL when deploying the UI only, e.g. https://api.example.com
VITE_API_BASE=
